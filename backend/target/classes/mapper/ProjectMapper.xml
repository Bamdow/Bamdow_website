<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bamdow.mapper.ProjectMapper">
    <insert id="insert" parameterType="com.bamdow.pojo.entity.Project">
        INSERT INTO projects (
            id,
            title,
            description,
            category,
            tags
        ) VALUES (
            #{id},
            #{title},
            #{description},
            #{category},
            #{tags}
        )
    </insert>
    <update id="update" parameterType="com.bamdow.pojo.entity.Project">
        update projects
        set
            title = #{title},
            description=#{description},
            tags=#{tags}
        where
            id=#{id}
    </update>


    <select id="pageQuery" resultType="com.bamdow.pojo.vo.ProjectQueryVO">
        SELECT
            id,
            title,
            description,
            category,
            tags
        FROM
            projects
        ORDER BY
            created_at
    </select>
</mapper>